/* groovylint-disable BlockEndsWithBlankLine, CompileStatic, FileEndsWithoutNewline */
pipeline {
    agent any
stages {
    stage ('Verify Branch') {
        steps {
        echo '$GIT_BRANCH'
        } 
    }
}
}